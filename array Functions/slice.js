//arr.slice(start,end) returns an array of new elements containing elements from start to end index
//Syntax:-   arr.slice(start,end)
let str="test"
console.log(str.slice(1,3));

//the return array doesnot include end value
//the original array doesnot gets changed